# Local development environment variables
# Copy this file to `.env.local` and fill in the values
# Next.js will automatically load `.env.local` (not committed to git)

# --- Database ---
# Example: mongodb://localhost:27017/global-live-chat-room
MONGODB_URI=

# --- Email (for admin notifications) ---
# Required to send email notifications when non-"arham" users post a message
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
# Optional: set to enable admin notifications. If unset, emails are disabled.
NOTIFY_EMAIL_TO=

# --- Misc ---
# Add any other local-only secrets or configs here

# --- Pusher (Realtime) ---
# Server-side credentials (do NOT expose in client)
PUSHER_APP_ID=
PUSHER_KEY=
PUSHER_SECRET=
PUSHER_CLUSTER=ap2

# Client-side (public) config for Pusher Channels
# Use the same cluster as above; key is public.
NEXT_PUBLIC_PUSHER_KEY=
NEXT_PUBLIC_PUSHER_CLUSTER=ap2
